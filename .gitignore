# Node modules (both frontend & backend)
node_modules/

# Environment files (keep secrets safe!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build and cache (React + Node)
build/
dist/
.cache/
*.tmp

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Coverage reports (from testing)
coverage/

# OS generated files
.DS_Store
Thumbs.db

# IDE / Editor settings
.vscode/
.idea/
*.swp

# Optional: lock files (if you want to avoid conflicts across package managers)
package-lock.json
yarn.lock
pnpm-lock.yaml
